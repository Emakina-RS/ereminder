import { storiesOf } from "@storybook/react";
import React from "react";
import InvalidLoginMessage from "./InvalidLoginMessage";

storiesOf("InvalidLoginMessage", module).add("default", () => <InvalidLoginMessage />);
